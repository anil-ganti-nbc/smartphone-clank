# Operator-local config overlay — never commit, never let an upgrade touch this.
config/config.local.yaml

# Secrets
.env

# Runtime data (root-level only — do not shadow knowledge/data/, which is
# tracked reference YAML, not runtime state)
/data/
logs/
runtime/backups/
runtime/logs/
runtime/pids/
runtime/reports/
runtime/diagnostics/

# Python
__pycache__/
*.pyc
.venv/
*.egg-info/

# Windows executables (defensive)
*.exe

# Local AI-tool config, not project content
.claude/

.venv312/

# PyInstaller build artifacts (native/windows/SmartphoneClank.spec)
/build/
native/windows/build/
/dist/
